import { Typography } from '@mui/material';

const Header = () => {
  return (
    <header>
      <Typography 
        variant="p"
        component="h1"
        align="center"
      >
        imovie
      </Typography>
    </header>
  )
}

export default Header;
